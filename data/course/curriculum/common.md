# High School Math Curriculum (Common Range)

This file is auto-generated from current TOPICS.
It represents the current in-repo curriculum baseline.

## 数学I
### 二次関数
- 二次方程式の解（基本） (`quad_solve_basic`)
- 判別式と解の個数（基本） (`quad_discriminant_basic`)
- 解と係数の関係（基本） (`quad_roots_relations_basic`)
- 二次不等式（基本） (`quad_inequality_basic`)
- 二次関数の最大・最小（基本） (`quad_maxmin_basic`)
- 二次関数のグラフと頂点 (`quad_graph_basic`)

### 三角比
- 三角比（直角三角形） (`trig_ratio_basic`)
- 特殊角の三角比 (`trig_special_angles_basic`)

### 図形と計量
- 直角三角形の計量（基本） (`geo_measure_right_triangle_basic`)
- 正弦定理・余弦定理（基本） (`geo_sine_cosine_law_basic`)

### データの分析
- データの代表値（平均・中央値・最頻値・範囲） (`data_summary_basic`)
- 分散・標準偏差（基本） (`data_variance_sd_basic`)
- 散布図と傾向（基本） (`data_scatter_basic`)
- 共分散（基本） (`data_covariance_basic`)
- 相関係数（基本） (`data_correlation_basic`)
- 回帰直線（基本） (`data_regression_basic`)

### 集合と論理
- 集合の基本と演算（基本） (`set_operations_basic`)
- 命題と条件（基本） (`prop_proposition_basic`)

### 数と式
- 数と式：展開（基本） (`algebra_expand_basic`)
- 数と式：因数分解（基本） (`algebra_factor_basic`)
- 数と式：一次方程式（基本） (`algebra_linear_eq_basic`)
- 数と式：一次不等式（基本） (`algebra_ineq_basic`)


## 数学A
### 場合の数
- 場合の数（和の法則・積の法則） (`combi_basic`)
- 順列（基本） (`combi_permutation_basic`)
- 条件付きの数え上げ (`combi_conditions_basic`)

### 確率
- 確率の基本 (`prob_basic`)
- 補集合と確率 (`prob_complement_basic`)
- 組合せと確率 (`prob_combi_basic`)

### 整数の性質
- 約数・倍数・最大公約数・最小公倍数（基本） (`int_divisor_multiple_basic`)
- 余りと剰余（基本） (`int_remainder_basic`)
- 素因数分解と素数判定（基本） (`int_prime_factor_basic`)
- 整除性の判定（基本） (`int_divisibility_tests_basic`)
- 最大公約数・最小公倍数の応用（基本） (`int_gcd_lcm_applications_basic`)
- 一次不定方程式（基本） (`int_diophantine_basic`)
- 剰余の入門（基本） (`int_mod_arithmetic_intro`)

### 図形の性質
- 比の定理 (`geo_ratio_theorems`)
- 円の性質 (`geo_circle_geometry`)
- 三角形の中心 (`geo_triangle_centers`)
- 2円の位置関係 (`geo_circle_relations`)

### proof
- 数学的帰納法（基本） (`induction_basic`)


## 数学II
### 指数・対数
- 指数・対数の計算（基本） (`exp_log_basic`)
- 指数・対数方程式（基本） (`exp_log_equations_basic`)
- 底の変換（基本） (`exp_log_change_base_basic`)
- 指数成長（基本） (`exp_log_growth_basic`)
- 定義域（基本） (`exp_log_domain_basic`)
- 指数方程式（同底）（基本） (`exp_log_simple_equation_basic`)
- 指数方程式（2x）（基本） (`exp_log_power_equation_basic`)
- 対数方程式（基本） (`exp_log_log_equation_basic`)
- 対数の加法（基本） (`exp_log_log_sum_basic`)
- 対数の減法（基本） (`exp_log_log_diff_basic`)
- 対数と指数の積（基本） (`exp_log_log_product_basic`)
- 対数の性質（基本） (`exp_log_property_basic`)

### 多項式
- 余りの定理（基本） (`poly_remainder_basic`)
- 因数定理（基本） (`poly_factor_k_basic`)
- 多項式の値（基本） (`poly_value_sum_basic`)
- 因数から係数（基本） (`poly_coeff_from_roots_basic`)
- 二項定理の係数（基本） (`binomial_coeff_basic`)
- 二項定理（x+y）（基本） (`binomial_xy_coeff_basic`)
- 二項定理の値（基本） (`binomial_value_basic`)
- 二項定理の中央項（基本） (`binomial_middle_coeff_basic`)

### 恒等式・不等式
- 恒等式の値（基本） (`identity_eval_basic`)
- 恒等式の係数（基本） (`identity_coefficient_basic`)
- 恒等式の係数比較（基本） (`identity_coeff_quadratic_basic`)
- 相加相乗（基本） (`inequality_mean_basic`)
- 和と積（基本） (`inequality_sum_product_basic`)
- 相加相乗の差（基本） (`inequality_amgm_basic`)

### 座標
- 直線の傾き（基本） (`coord_line_slope_basic`)
- 直線の切片（基本） (`coord_line_intercept_basic`)
- 平行・垂直の傾き（基本） (`coord_line_parallel_perp_basic`)
- 点と直線の距離（基本） (`coord_distance_point_line_basic`)
- 円の半径（基本） (`coord_circle_radius_basic`)
- 円の中心（基本） (`coord_circle_center_basic`)
- 領域（基本） (`coord_region_basic`)

### 三角比
- 三角恒等式（基本） (`trig_identities_basic`)
- 三角恒等式（1）（基本） (`trig_identity_pythag_basic`)
- 振幅（基本） (`trig_amplitude_basic`)
- 位相のずれ（基本） (`trig_phase_shift_basic`)
- 上下移動（基本） (`trig_vertical_shift_basic`)
- 値域（基本） (`trig_graph_range_basic`)
- 周期（グラフ）（基本） (`trig_graph_period_basic`)
- 中心線（基本） (`trig_graph_midline_basic`)
- 最大値（基本） (`trig_graph_max_basic`)
- 最小値（基本） (`trig_graph_min_basic`)
- y切片（基本） (`trig_graph_intercept_basic`)
- 三角方程式（弧度）（基本） (`trig_equation_radian_basic`)
- tanの値（基本） (`trig_identity_tan_basic`)
- tanの恒等式（基本） (`trig_identity_tan_relation_basic`)
- 加法定理（基本） (`trig_addition_basic`)
- 倍角（基本） (`trig_double_angle_basic`)
- 周期（基本） (`trig_period_basic`)
- 弧度法（基本） (`trig_radian_basic`)
- 三角方程式（基本） (`trig_equations_basic`)

### 微分積分
- 微分の計算（基本） (`calc_derivative_basic`)
- 一次関数の微分（基本） (`calc_derivative_linear_basic`)
- 積分の計算（基本） (`calc_integral_basic`)
- 接線の傾き（基本） (`calc_tangent_slope_basic`)
- 接線の式（基本） (`calc_tangent_line_basic`)
- 増減（基本） (`calc_increasing_basic`)
- 平均値（基本） (`calc_average_value_basic`)
- 直線間の面積（基本） (`calc_area_between_lines_basic`)
- 定積分（一次関数）（基本） (`calc_integral_linear_basic2`)
- 放物線と直線の面積（基本） (`calc_area_between_parabola_basic`)
- 直線下の面積（基本） (`calc_area_under_line_basic`)
- 接点の座標（基本） (`calc_tangent_value_basic`)
- 定数の積分（基本） (`calc_integral_constant_basic`)
- 定積分（基本） (`calc_integral_sum_basic`)
- 2次関数の積分（基本） (`calc_integral_quadratic_basic`)
- 極値（基本） (`calc_extrema_basic`)
- 面積（基本） (`calc_area_basic`)


## 数学B
### 数列
- 等差数列の一般項（基本） (`seq_arithmetic_basic`)
- 等比数列の一般項（基本） (`seq_geometric_basic`)
- 等比数列の平均（基本） (`seq_geometric_mean_basic`)
- 和から項（基本） (`seq_term_from_sum_basic`)
- 等比数列の和（基本） (`seq_geometric_sum_n_basic`)
- 公比（項から）（基本） (`seq_common_ratio_from_terms_basic`)
- 数列の和（基本） (`seq_sum_basic`)
- 公差の計算（基本） (`seq_common_difference_basic`)
- 等比数列の和（基本） (`seq_geometric_sum_basic`)
- 等比数列の無限和（基本） (`seq_geometric_limit_basic`)
- 等差数列の和（基本） (`seq_arithmetic_sum_basic`)
- 等差数列の和（項から）（基本） (`seq_arithmetic_sum_from_terms_basic`)
- 等差数列の平均（基本） (`seq_arithmetic_mean_basic`)
- 公差（項から）（基本） (`seq_arithmetic_diff_from_terms_basic`)
- 漸化式（基本） (`seq_recurrence_basic`)
- 漸化式の項（基本） (`seq_recurrence_term_basic`)

### 統計
- 標本平均（基本） (`stats_sample_mean_basic`)
- 標本平均の平均（基本） (`stats_sampling_mean_basic`)
- 標準誤差（基本） (`stats_standard_error_basic`)
- 信頼区間の幅（基本） (`stats_confidence_interval_basic`)
- 散布図（基本） (`stats_scatter_basic`)
- 共分散（基本） (`stats_covariance_basic`)
- 相関係数（基本） (`stats_correlation_basic`)
- 回帰直線（基本） (`stats_regression_basic`)
- 統計的推定（基本） (`stats_inference_basic`)

### ベクトル
- ベクトルの演算（基本） (`vector_operations_basic`)
- 内積（基本） (`vector_inner_basic`)
- ベクトルの長さ（基本） (`vector_length_basic`)
- 単位ベクトル（基本） (`vector_unit_basic`)
- 平行条件（基本） (`vector_parallel_basic`)
- 成分（基本） (`vector_component_basic`)
- 直線上の点（基本） (`vector_line_point_basic`)
- 直交条件（基本） (`vector_orthogonal_condition_basic`)
- 平面との距離（基本） (`vector_distance_plane_basic`)
- 法線ベクトル（基本） (`vector_plane_normal_basic`)
- 中点（基本） (`vector_midpoint_basic`)
- 内積と角（基本） (`vector_inner_from_angle_basic`)
- 内分・外分（基本） (`vector_section_basic`)
- 空間ベクトル（基本） (`vector_space_basic`)
- 空間距離（基本） (`vector_distance_basic`)
- 平面の方程式（基本） (`vector_plane_basic`)
- 直交条件（基本） (`vector_orthogonality_basic`)
- ベクトルのなす角（基本） (`vector_angle_basic`)
- 射影（基本） (`vector_projection_basic`)
- 直線のパラメータ（基本） (`vector_line_basic`)
- 面積（基本） (`vector_area_basic`)
- ベクトル方程式（基本） (`vector_equation_basic`)


## 数学C
### 複素数
- 複素数の計算（基本） (`complex_basic`)
- 複素数の絶対値（基本） (`complex_modulus_basic`)
- 複素数平面（基本） (`complex_plane_basic`)
- 共役複素数（基本） (`complex_conjugate_basic`)
- 複素数の回転（基本） (`complex_rotation_basic`)
- 極形式（基本） (`complex_polar_basic`)
- 複素数平面の距離（基本） (`complex_distance_basic`)
- 複素数平面の中点（基本） (`complex_midpoint_basic`)
- 複素数の平方（実部）（基本） (`complex_square_real_basic`)
- iの累乗（基本） (`complex_power_i_basic`)
- 共役積（基本） (`complex_conjugate_product_basic`)
- 絶対値の二乗（基本） (`complex_modulus_square_basic`)
- 偏角（軸上）（基本） (`complex_argument_axis_basic`)
- 偏角（象限）（基本） (`complex_argument_quadrant_basic`)
- 偏角（度数法）（基本） (`complex_argument_degree_basic`)
- 極形式の実部（基本） (`complex_polar_value_basic`)
- ド・モアブル（基本） (`complex_de_moivre_basic`)
- n乗根の偏角（基本） (`complex_root_unity_basic`)
- 積の実部（基本） (`complex_multiply_real_basic`)
- 積の虚部（基本） (`complex_multiply_imag_basic`)
- 積の絶対値（基本） (`complex_modulus_product_basic`)
- 絶対値（基本） (`complex_equation_abs_basic`)
- 実部（基本） (`complex_equation_real_imag_basic`)
- 共役の和（基本） (`complex_equation_conjugate_basic`)
- 回転の実部（基本） (`complex_rotation_real_basic`)
- 回転の虚部（基本） (`complex_rotation_imag_basic`)
- 180度回転（基本） (`complex_rotation_180_basic`)
- 除法の実部（基本） (`complex_division_real_basic`)
- 絶対値の和（基本） (`complex_modulus_sum_basic`)
- 極形式の虚部（基本） (`complex_polar_imag_basic`)
- 共役の絶対値（基本） (`complex_conjugate_modulus_basic`)
- 和の絶対値（基本） (`complex_add_modulus_square_basic`)
- 差の絶対値（基本） (`complex_sub_modulus_square_basic`)
- 三角形の面積（基本） (`complex_triangle_area_basic`)
- 中点と距離（基本） (`complex_midpoint_distance_basic`)
- 平行条件（基本） (`complex_parallel_condition_basic`)
- 直交条件（基本） (`complex_perpendicular_condition_basic`)
- 軌跡の半径（基本） (`complex_locus_circle_radius_basic`)
- 軌跡の中心（基本） (`complex_locus_circle_center_basic`)
- 偏角の加法（基本） (`complex_argument_product_basic`)
- 偏角の減法（基本） (`complex_argument_quotient_basic`)
- 90度回転（基本） (`complex_rotation_90_matrix_basic`)
- 偏角のべき（基本） (`complex_argument_power_basic`)
- 絶対値のべき（基本） (`complex_modulus_power_basic`)
- 垂直二等分線（基本） (`complex_locus_bisector_basic`)
- 垂直二等分線（実軸）（基本） (`complex_locus_vertical_line_basic`)
- 共役の偏角（基本） (`complex_argument_conjugate_basic`)
- 逆数の偏角（基本） (`complex_argument_inverse_basic`)
- 垂直二等分線（虚軸対称）（基本） (`complex_locus_horizontal_line_basic`)
- 内分点（基本） (`complex_section_internal_basic`)
- 外分点（基本） (`complex_section_external_basic`)
- 線分上の点（基本） (`complex_line_point_basic`)
- 偏角（基本） (`complex_argument_basic`)

### 二次曲線
- 円の方程式（基本） (`conic_circle_basic`)
- 円の中心（基本） (`conic_circle_center_basic`)
- 交点の判別（基本） (`conic_intersection_basic`)
- 接線の条件（基本） (`conic_tangent_basic`)
- 放物線の基本（焦点） (`conic_parabola_basic`)
- 放物線の準線（基本） (`conic_parabola_directrix_basic`)
- 放物線の焦点（基本） (`conic_parabola_focus_basic`)
- 放物線の準弦（基本） (`conic_parabola_latus_rectum_basic`)
- 放物線の接線の傾き（基本） (`conic_parabola_tangent_slope_basic`)
- 放物線の頂点（基本） (`conic_parabola_vertex_basic`)
- 円の半径（基本） (`conic_circle_radius_basic`)
- 楕円の軸（基本） (`conic_ellipse_axis_basic`)
- 双曲線の頂点（基本） (`conic_hyperbola_vertex_basic`)
- 楕円の基本（長軸） (`conic_ellipse_basic`)
- 楕円の長軸（基本） (`conic_ellipse_major_axis_basic`)
- 楕円の短軸（基本） (`conic_ellipse_minor_axis_basic`)
- 楕円の接線（基本） (`conic_ellipse_tangent_basic`)
- 楕円の焦点（基本） (`conic_ellipse_focus_basic`)
- 双曲線の基本（焦点） (`conic_hyperbola_basic`)
- 双曲線の漸近線（基本） (`conic_hyperbola_asymptote_basic`)
- 双曲線の漸近線の傾き（基本） (`conic_hyperbola_asymptote_slope_basic`)
- 双曲線の実軸（基本） (`conic_hyperbola_transverse_axis_basic`)
- 円の接線の傾き（基本） (`conic_circle_tangent_slope_basic`)
- 円と直線の交点（基本） (`conic_line_intersection_count_basic`)
- 円の一般形の半径（基本） (`conic_circle_general_radius_basic`)
- 円の一般形の中心（基本） (`conic_circle_general_center_basic`)
- 双曲線の焦点間距離（基本） (`conic_hyperbola_foci_distance_basic`)
- 楕円の焦点距離（基本） (`conic_ellipse_c_basic`)
- 双曲線の焦点距離（基本） (`conic_hyperbola_c_basic`)
- 放物線と直線の交点（基本） (`conic_parabola_line_intersection_count_basic`)
- 楕円の代入値（基本） (`conic_ellipse_value_basic`)
- 双曲線の代入値（基本） (`conic_hyperbola_value_basic`)
- 放物線の焦点（一般形）（基本） (`conic_parabola_general_focus_basic`)
- 放物線の頂点（平行移動）（基本） (`conic_parabola_vertex_shift_basic`)
- 円の接点（基本） (`conic_circle_tangent_point_basic`)
- 放物線の焦点（縦）（基本） (`conic_parabola_focus_vertical_basic`)
- 放物線の準線（縦）（基本） (`conic_parabola_directrix_vertical_basic`)
- 楕円の焦点間距離（基本） (`conic_ellipse_focus_distance_basic`)
- 円上の点判定（基本） (`conic_circle_point_on_basic`)
- 放物線の接線の切片（基本） (`conic_parabola_tangent_intercept_basic`)
- 楕円の中心（基本） (`conic_ellipse_center_basic`)
- 双曲線の中心（基本） (`conic_hyperbola_center_basic`)
- 双曲線の漸近線の式（基本） (`conic_hyperbola_asymptote_equation_basic`)

### 確率
- 二項分布の期待値（基本） (`prob_binomial_expectation_basic`)
- 二項分布の確率（基本） (`prob_binomial_probability_basic`)
- 二項分布の分散（基本） (`prob_binomial_variance_basic`)
- 正規分布の平均・分散（基本） (`normal_distribution_basic`)
- 標準化（基本） (`normal_standardization_basic`)
- 標準化の逆（基本） (`normal_backsolve_basic`)


## 数学III
### 微分積分
- 極限（基本） (`calc_limit_basic`)
- 連続性（基本） (`calc_continuity_basic`)
- 微分法（応用） (`calc_derivative_advanced_basic`)
- 積分法（応用） (`calc_integral_advanced_basic`)
- 曲線で囲まれた面積（基本） (`calc_curve_area_basic`)
- 媒介変数表示（基本） (`calc_parametric_basic`)

